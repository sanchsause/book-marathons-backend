meta:
  environment: nodejs
  toolchain:
    version: 20.x

build:
  commands:
    - npm ci
    - npx prisma generate
    - npx prisma migrate deploy
    - npm run build

run:
  command: node dist/index.js