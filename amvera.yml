meta:
  environment: node
  toolchain:
    name: npm
    version: 20.x

build:
  additionalCommands: npm ci && npx prisma generate && npm run build
  artifacts:
    "*": /

run:
  command: npx prisma db push --skip-generate && node dist/index.js
  containerPort: 3000