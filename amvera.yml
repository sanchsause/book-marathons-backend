meta:
  environment: nodejs
  toolchain:
    version: 20.x

build:
  commands:
    - npm ci
    - npx prisma generate
    - npm run build

run:
  command: npx prisma db push --skip-generate && node dist/index.js
  volumes:
    - /data